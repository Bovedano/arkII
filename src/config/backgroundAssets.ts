/** Background images available in public/assets/backgrounds. Vite can't glob the public/ dir
 *  at build time, so add the filename here whenever a new image is dropped in that folder —
 *  PreloadScene and the level editor's background picker both read from this list. */
export const BACKGROUND_ASSETS: string[] = [];
